## v0.1.101 — 待还原提示不再每次启动都刷（2026-09-04）

- **「有待还原的网络服务」从警告降为普通消息，并且跨启动不再重复。** 这是设计中的等待状态
  （VPN 类虚拟网络服务随其 App 启停出现/消失，不在列表里就写不回去，快照留着等它回来），
  不是需要用户处理的故障。原来判重只存在内存里，服务长期缺席时**每次启动都重报一条**——
  真机 2026-09-04 的 `LAN` 服务从系统里消失后，20:01 / 20:06 / 20:08 三次启动各刷了一对，
  而它可能再也不会回来。现在判重状态落盘：同一批服务只提示一次；这批变了、或者清空后再次缺席才重新提示。

## v0.1.100 — 开着代理和 TUN 也能切配置（2026-09-04）

- **修复：系统代理 + TUN 都开着时切换配置必然失败并回滚。** 真机 2026-09-04 10:13 连续两次
  「当前配置应用失败，已回滚：启用系统代理失败且自动恢复失败：networksetup（8）Unable to find
  item in network database」，用户必须先关掉代理和 TUN 才切得动，而这期间内核一直跑着旧配置
  （网页分流测试显示的仍是旧订阅节点的出口）。两处原因，都已修：
  - **`enable()` 无条件重写系统代理**：切配置会重走一遍启动流程，而中转端口是固定的、绕过列表
    通常也没变，却仍要先还原、再采集、再写入，4 个网络服务就是几十条 `networksetup`。现在先读回
    当前设置，已经指向本机同一端口且绕过一致时**一条写入都不发**，也不动已有快照（快照记的是
    用户接管前的原始设置，重新采集会把它覆盖成"指向我们自己"，还原就再也回不去了）。
  - **瞬时错误的重试预算太短**：TUN 拆建会让网络服务列表抖好几秒，而重试只有 200+400ms。
    预算扩到约 3 秒（200/400/800/1600ms）。系统 DNS 那一步此前**完全没有重试**，与系统代理
    前后脚跑、撞同一段抖动，现在补齐同一套（只修代理的话它就是下一个失败点）。
- **连接页更省电**：连接页和仪表盘订阅的是同一个 `/connections` 端点，此前两条流各跑一遍
  JSON 解析——120~160 条连接的 payload 每秒被解析两次。真机 2026-09-04 连接页开着 6 小时，
  CPU 均值 3~7.7%、峰值 12.6%，而主线程只占 48%，另一半正是这份重复解析。现在合成一条流：
  连接页开着时由它兼供总量，仪表盘那条订阅收起；页面关闭时交还，会话累计流量不受影响。

# 版本历史

空山的 GitHub Releases 保留全部历史版本，可随时回退到任一旧版安装包。下面按版本归纳用户可见的变化，不等同于逐条提交日志。

## v0.1.99 — 停止接管后不再留下"指向空端口"的系统代理（2026-09-04）

- **修复：停止系统代理 / TUN 时，此刻不在网络服务列表里的服务被静默跳过、快照也被删掉。**
  真机 2026-09-03：Shadowrocket 退出时它的虚拟网络服务从列表消失，kongshan 停止代理时漏掉了它；
  它回到列表后三项代理仍指向已停的 `127.0.0.1:36815`——用户关了代理，Codex 等直连请求全被送进
  空端口报 404，系统设置却显示代理已关。现在这类服务保留为"待还原"，快照不删，服务重新出现
  （启动 / 换网）时自动复位，并记一条消息事件；期间再次开启代理也不再被"尚未恢复的快照"拒绝。
  系统 DNS 的还原有同一处漏洞，一并修复。
- **还原后读回核对**：`networksetup` 返回成功不再等于还原成功；读回仍不一致的服务留在快照里重试，
  错误信息点名服务与差异。
- **不依赖快照的残留清扫**：启动和换网时，对没在接管的那类做检查——任何仍指向 kongshan 中转端口的
  系统代理、仍指向 TUN 劫持地址的系统 DNS 都会被关掉 / 摘除（用户自己配的其它代理、DNS 不动），
  并记一条含服务名与端口的消息事件。只对曾接管过的安装生效，从未开过对应模式不会去碰 `networksetup`。
- **回滚路径不再吞掉还原失败**：配置应用失败又回滚失败时，系统代理 / DNS / SSH 配置未能还原的情况
  会写进错误信息与消息事件，不再只显示"应用失败"。
- **DNS 规则**：反向解析（`*.in-addr.arpa` / `*.ip6.arpa`）与 Bonjour 发现（`_dns-sd._udp`）查询
  留在本地解析器（有内网 DNS 交给内网 DNS，否则国内解析器），不再经代理去问公共 DNS——
  真机一天 73 条此类失败，且会把内网网段的查询送出去。
- **仪表盘开着时更省电**：速率、活跃连接、内核内存、空山占用、出口检测各自成小视图，
  值变时只刷新自己那一小块，不再整页重排。真机指标流水：仪表盘可见时 CPU 中位 6.3%、
  其它页 1.5%，差的这一截主要来自整页跟着速率每两秒重排。
- **CPU 异常时自动保存调用栈**：异常持续 45 秒以上时用系统 `sample` 给自己采 5 秒，
  存到 `Application Support/kongshan/samples/`（10 分钟最多一份、只留最近 5 份），
  路径写进消息事件。此前告警只说得出"主线程在渲染仪表盘"，说不出主线程在跑什么。
- **界面打磨**：指标卡的数值紧贴说明文字，不再吊在卡片底边；代理页、配置页去掉交替行底色
  （条目少时条纹会铺满空白画出一排"幽灵行"，也是"代理这一列比较暗"的来源）；代理页只给选中
  节点放标记，其余行不再各放一个机架符号；规则页的分应用代理改为两行（App 名不再被截断），
  规则浏览器加了「订阅规则 · 只读 · 按目标策略分组」标题，分组名改为色点 + 正文色。

## v0.1.98 — 局域网共享独立成页，可配置、可看用量（2026-09-03）

- **侧栏新增「共享」页**，局域网共享从设置里的一个开关变成完整模块：开关、连接地址、
  配置、已接入设备四块。
- **入口改为独立监听、独立端口**（默认 7890）。上一版是把本机代理入口改绑到全部接口，
  每次开关都会掐断你自己正在走的连接；现在两个入口互不相干，开关共享不再影响本机代理。
- **可配置**：监听端口，以及来源白名单（如 `192.168.1.0/24` 或单个 `192.168.1.7`）。
  白名单留空＝允许全部私网地址；**无论怎么配都不接受公网来源**。
- **已接入的设备**：逐个列出对方 IP、当前连接数、实时上下行速率与累计流量；
  断开的设备仍留在列表里显示累计用量，直到关闭共享。

## v0.1.97 — 局域网共享，以及三处界面反馈的修正（2026-09-03）

- **新增局域网共享**（设置 → 隧道，默认关闭）。开启后同网段的手机、平板、另一台电脑
  把 HTTP 与 SOCKS5 代理都填成「本机 IP:端口」，就能走本机的分流与节点；
  设置页直接列出可填的地址并可一键拷贝。
  只接受私网来源（10/172.16-31/192.168 网段），机器拿到公网 IP 时不会变成互联网上的
  开放代理；但同网段内不做身份校验，公共 Wi-Fi 请谨慎。
  切换开关会重建代理入口，当前经由本机代理的连接会断开一次。
- **首页补齐两张卡**：新增「当前配置」（名称与节点数）与「空山占用」（自身 CPU 与内存）。
  网格列数改为只取 6 的因数，任何窗口宽度下最后一行都不会再缺角。
- **仪表盘明显更省电**：运行时长从每秒刷新改为每分钟，数值不再做字号自适应缩放，
  流量曲线改用直线插值。真机实测仪表盘开着时占用 4~10%，这三处都是每秒重新布局的来源。
- **代理页左侧策略列不再是一块灰底**：那是侧栏材质用错了地方，改成与右侧同形的普通列表。
- **连接页列不再需要横向翻**：默认列收窄到最小窗口也能放下，两个「累计」列默认隐藏，
  右键表头可以勾出来；列宽与显隐跨会话记住。规则·链路悬停可看完整内容。

## v0.1.96 — 界面改为原生 macOS 形态（2026-09-03）

主窗口从"Surge/Stash 式仪表盘"改成 macOS 系统应用的样子。业务逻辑零改动。

- **真标题栏与统一工具栏**：每页的标题、统计（如「连接 — 12 条 · ↑ 1.2 MB/s」）和操作按钮
  都进了系统标题栏，不再各页自绘一条页头。
- **搜索进工具栏**：连接、内核日志、代理、规则四页改用系统搜索框（⌘F 可达）。
- **连接页改为真正的表格**：列可排序、列宽可拖、右键菜单不变，单击一行看完整链路；
  上千条每秒刷新也比之前省。
- **代理页节点改为列表**：一行一个节点、点一下就切换，协议与延迟固定在行末对齐；
  出站模式、排序、测速都在工具栏。
- **规则页上半改为系统分组表单**：规则开关、分应用代理、强制代理、SSH、命中测试五个分区，
  后四个可折叠；已添加的规则按行列出，逐条可删。
- **去掉所有投影、渐变图标块和描边卡片**；容器一律用系统 GroupBox / 分组表单 / 列表；
  字号改用系统文本样式，跟随系统设置。侧栏加宽到 macOS 规范范围，未读数用系统徽标。
- 托盘弹窗：策略选择改为系统弹出按钮并对齐成两列，底部改为链接式按钮。
- 设计依据与逐页方案记录在 `docs/design/NATIVE_UI.md`。

## v0.1.95 — 运行指标连续记账，不再只在出事时留一笔（2026-09-03）

- **新增运行指标流水**：每分钟往 `metrics.ndjson` 记一行——CPU 均值与峰值、user 与
  主线程占比、内存、线程数、当时在哪个页面、哪些推送在跑、活跃连接数、网卡收发量，
  以及连接列表/流量图/日志三个刷新源各发布了多少次。
  此前只在越过阈值时留一条告警，于是"低于阈值却一直存在"的开销完全看不见——
  真机上主窗口开着时稳定 5~7%，一条记录都不会有。
  现在把这条时间线拉出来就能回答"从什么时候开始涨的、涨的时候在做什么"。
  文件不受消息页「全部清除」影响，约两周历史，诊断导出会带上近 4 小时。
- **不再把「本机没网」报成「节点故障」**。此前机器休眠或断网期间的失败会全算到
  当时选中的节点头上，还建议你去换节点——换了也没用。现在同时看直连的成败：
  直连也在大面积失败就说明是本机网络不通，提示会明说「换节点无用」。
- **建连失败特征补上 `no route to internet`**。断网期间 2,907 条直连失败此前
  一条都识别不出来，上面那个判据没有它就形同虚设。
- 失败原因不再因为残留右括号被劈成两类（`no route to internet` 与
  `no route to internet)` 曾被当成不同原因分别计数）。
- 时间跨度不足 1 分钟改写秒，不再出现「0 分钟内」这种读不懂的记录。

## v0.1.94 — 故障检测不再依赖你正好开着日志页（2026-09-02）

- **两个检测器此前几乎全程没有输入**。内核日志流原本挂在「内核日志」页的开关上，
  只有你盯着那一页时，DNS 停摆与节点建连失败才会被统计。真机后果：09-02 晚上
  节点连续 276 次建连全部失败（100%），52 秒后你手动关掉代理，而消息页一条记录都没有。
  现在日志流随代理常开，日志页只决定要不要把行显示出来。
  没开日志页时先用最廉价的判据挡掉无关行，不额外耗 CPU。
- **内核停止时先结算再清空统计**。此前直接丢弃未到期的窗口，而故障最典型的形态
  恰恰是「一开就全失败、几十秒内被手动关掉」——窗口远没到 10 分钟，证据却被丢干净。
  达不到阈值仍然不报，不会因为停止就凭空多出记录。
- **日志导出补上 TUN 内核日志**。TUN 由助手以 root 启动，日志写在助手自己的目录，
  此前导出完全读不到——30 小时的 TUN 会话在导出里是一片空白。大文件只取尾部。
- **助手的 TUN 日志不再无限增长**。此前只在助手启动时轮转一次，长会话期间一直涨，
  真机实测已到 63 MB（限额 5 MB）。现在运行期间也会定期轮转。

## v0.1.93 — 告警不再会被一键清掉（2026-09-01）

- **警告与错误另存一份**到 `~/Library/Application Support/kongshan/diagnostics.ndjson`，
  消息页的「全部清除」不再影响它。此前 CPU 异常这类取证记录与日常消息共用一个清空按钮，
  真机上因此丢掉了 99.8% 的异常证据——4 天里累计 21 小时 CPU，只有 151 秒可归因。
  消息页顶部会直接写出存档路径，清除前就知道证据还在。
- **告警会发系统通知**。同一标题 10 分钟内只提醒一次，避免连发时刷屏；
  已自愈的事件（内核崩溃后一秒内自动重启）和已经另发过通知的事件不重复打扰，
  但**存档照留**。
- CPU 异常记录补上「当前页面」与「活跃订阅」两项。此前只说得出「主线程在渲染」，
  说不出在渲染哪一页，下次复发仍会卡在同一处。
- 「DNS 解析长期零星超时」的判据从 1 小时 ≥8 次放宽到 6 小时 ≥5 次。
  真机 18 小时里 17 次超时、每次卡满 10 秒（累计白等约 170 秒），
  按原阈值折算下来一次都报不出来。

## v0.1.92 — 直连域名不再绕道代理解析（2026-08-27）

- **旁路（直连）的域名此前是经代理去问远端 DNS 的**，解析绕远一圈：既跟着代理一起抖，
  拿回来的地址还可能本地根本连不通。现在它们和内网域名一样交给国内解析器。
  真机实测：受影响的内网域每次卡满 10 秒超时，2 小时 44 分内失败 26 次。
- 解析结果改为**优先 IPv4**。直连路径常常没有 IPv6 出口，解析器却照样返回 AAAA，
  内核挑中它就得到「网络不可达」或干耗到超时。只调顺序，纯 IPv6 的目标照常可达。
- 自定义规则里指向代理的域名，解析优先级与路由保持一致，不会被上面两条改掉走向。
- 启用系统代理时若撞上 macOS 网络服务列表正在变动（换网、唤醒后常见），
  会自动重试而不是直接失败回滚。此前会连报两次「配置应用失败，已回滚」，
  且回滚本身也失败，系统代理状态一度不确定。
- 消息页新增「DNS 解析长期零星超时」：此前只在两分钟内连续超时才提示，
  而「每几分钟卡一次、每次 10 秒」这种更难受的情况一条记录都没有。
- 合并重复的「系统已唤醒」记录（macOS 会发出没有对应真实唤醒的通知）。

## v0.1.91 — 开机后自动恢复系统代理（2026-08-25）

- 设置 → 开机自启新增「登录后自动恢复上次的接管」（默认关闭）。开启后，若关机前开着
  系统代理，登录时会等网络就绪再自动开启。
- 只认**正常退出**时的状态：崩溃或断电不会被当成"你想要的状态"反复恢复。
- 网络在 20 秒内不可达则不接管，并发系统通知说明；自动接管失败同样发通知——
  登录时主窗口不显示，界面上的提示看不见。
- 当前版本**不自动恢复 TUN**：助手需重装时它会弹管理员密码框，开机瞬间弹框体验很差。
  上次用的是 TUN 时整体跳过，并在消息页留下记录。

## v0.1.90 — 界面refresh（2026-08-23）

- 通知提醒从常驻横幅改为窗口右上角的原生工具栏按钮，点开是紧凑弹窗；侧边栏「消息」
  带未读数字徽标。界面不再被提醒条挤占一行。
- 节点卡片改为上下两段式：首行专供节点名与国旗，长节点名不再把延迟挤走；延迟徽标
  升级为带背景和边框的胶囊，超时另加警告图标。
- 统一圆角与描边规范，图标改用更贴切的 SF Symbols，策略组按用途识别更多类型
  （AI、GitHub、Google、广告拦截、故障转移等）。

## v0.1.82 — DNS 引导解析器可配置（2026-08-23）

- 设置 → DNS 高级设置新增「引导解析器」。它负责解析出站节点自身的域名，留空时跟随
  国内 DoH 的地址（与此前行为一致）；填入独立 IP 可与国内 DoH 解耦，避免一台上游抖动
  同时影响节点域名与国内域名解析。

## v0.1.81 — 出事时说得出原因（2026-08-23）

- 失败类运行事件补齐原因：内核启动失败、配置/规则/DNS/TUN 应用失败、回滚成功与回滚也失败、
  内核意外退出、自动恢复失败等 13 处此前只记「失败了」不记「为什么」，现在都带上具体错误。
- 「内核已停止」记录触发方（用户关闭、切换配置、切换接管方式、测速结束、应用退出），
  不再与异常停止混为一谈。
- 新增节点建连失败聚合：某个节点在一段时间内建连失败偏多时，消息页直接给出节点名、
  失败比例与主要原因，不必再翻内核日志。规则拒绝（广告拦截）与直连不计入，
  配置重载导致的取消也不计入。
- 消息页运行事件新增「只看问题」筛选。
- 内核重启时清空 DNS 与出站失败统计，避免跨两代内核误判成一簇持续故障。

## v0.1.80 — 资源纪律修复（2026-08-21）

- 修复自诊断采样器的 mach port 引用泄漏：`task_threads` 返回的线程 send right 逐一归还，
  避免约 11 天后引用计数溢出使线程数指标静默失效，以及线程池短命线程留下死名字。
- 删除订阅时一并删除其缓存 YAML（内含节点凭据）；启动时兜底清理不再对应任何订阅的
  孤儿缓存（真机曾积累 5 个共 27 MB）。
- 深度资源排查结论：运行中堆泄漏仅 14 KB 且全部来自系统框架；FD、端口、线程、RSS 均无
  增长趋势；relay、日志存储、API 流的释放路径逐一复核通过。

## v0.1.79 — 修复后台 CPU 燃烧（2026-08-20）

- 修复菜单栏迷你面板关闭后 SwiftUI 宿主继续常驻观察应用状态的问题：面板改为开时新建、
  关时释放。此前开过一次面板后，速率每秒的变化会驱动不可见视图持续求值。
- 移除仪表盘与迷你面板里挂在每秒变化数值上的弹簧动画：动画在下一次采样前无法收敛，
  会按屏幕刷新率持续插值字形。真机代价为平均 57% CPU 连烧 8 小时、峰值内存 602 MB。
- v0.1.78 引入的运行期自诊断捕获并定位了上述问题；本版同时把它的 CPU 告警改为指数退避，
  修正活跃连接数读数。
- 并入 v0.1.74–0.1.77 的界面轮次：菜单栏迷你仪表盘、仪表盘主区重做、紧凑信息密度、
  自适应布局、规则页分区折叠。

## v0.1.73 — SSH 指定目标走代理（2026-08-14）

- 规则页新增“SSH 走代理”：按精确 IP + TCP 端口保存目标，OpenSSH 通过 ProxyCommand 经当前节点连接；支持 IPv4/IPv6、同 IP 多端口，同 IP 的其他端口不受影响。
- 只管理 `~/.ssh/config` 顶部带标记的 Include 和独立托管文件，原子写入、0600/0700 权限，符号链接与异常标记一律拒绝修改；不读取用户名、密码或私钥。
- 代理关闭时规则只保存在本机，开启后自动生效；停止、退出或启动失败时自动撤下，不留指向失效端口的配置。
- 修复仪表盘监控测试在发布门禁单独重跑时的偶发失败。

## v0.1.72 — 稳定入口与轻量交互（2026-08-09）

- 新增 App 用户态 TCP relay，系统代理始终连接稳定的本地端口；系统代理与 TUN 往返时只切换内核后端，避免端口争抢和短暂断网。
- 新增强制代理域名、IP 和 CIDR，并补充规则命中测试、连接出站链路与快捷规则操作。
- 批量测速改为有界并发、分批刷新并支持取消；配置、规则和节点切换减少不必要的内核重启。
- 仪表盘、日志和实时数据改为节流、合并发布与按可见性运行，菜单栏保留整机网速但不再周期重建展开的菜单。
- 补强发布门禁、安全替换、配置备份和系统代理/DNS 恢复验证。

## v0.1.67 — 原生菜单与代理页性能（2026-08-04）

- 状态栏改用 AppKit 原生菜单，保留实时速度，修复节点子菜单闪烁、跳动和“打开面板”失效。
- 修复策略组图标、右上角按钮被截断以及无订阅规则时的规则页显示。
- 节点测速、配置切换和代理开关脱离主线程重型工作，降低整页重绘和无界并发带来的卡顿。
- URL Test 任务与页面生命周期绑定，离开页面后及时停止不再需要的刷新。

## v0.1.59 — TUN IPv6 兼容（2026-07-30）

- 修复将 ULA 内网 IPv6 误判为公网 IPv6 的问题，解决 TUN 下部分应用上传失败。

## v0.1.58 — 单实例保护（2026-07-30）

- 阻止多个空山副本同时接管系统代理，避免竞态修改系统网络设置。

## v0.1.57 — 菜单栏整机网速（2026-07-30）

- 状态栏速度改为读取整机物理网卡，代理关闭时也可显示，并避免重复统计 TUN 流量。
- 调整代理页和设置页的布局、说明与操作反馈。

## v0.1.54 — 全流程稳定性走查（2026-07-30）

- 完成从启动、导入、切换、测速到退出恢复的真机走查，集中修复 11 处交互、状态同步和异常路径问题。

## v0.1.52 — 成品化打磨（2026-07-30）

- 更新应用图标，增加会话流量展示和多链接批量导入。
- 改善日志、规则和连接页的信息密度，减少重复与噪声。

## v0.1.51 — 内网 DNS 分流（2026-07-30）

- TUN 启动时探测局域网 DNS，为企业内网、家庭设备和反向解析生成独立路由。
- 节点域名解析改用无连接 UDP，避免长连接 DoH 失效导致的周期性停顿。

## v0.1.45 — 系统代理端口稳定（2026-07-28）

- 优先复用持久化的 mixed 端口，减少内核重启时系统代理反复重连。
- 这一阶段的端口复用仍受 root/user 绑定竞争影响，最终由 v0.1.72 的稳定 relay 架构完整解决。

## v0.1.43 — 免密码 TUN 全链路（2026-07-28）

- 打通特权助手的安装、启停、状态检查和异常恢复，日常使用 TUN 不再重复输入密码。
- 增加换网、睡眠唤醒、内核崩溃和孤儿进程恢复。

## v0.1.30 — TUN 真机网络修复（2026-07-23）

- TUN 固定使用 gVisor + Fake-IP，系统 DNS 指向 TUN 接口自身，解决开启 TUN 后网页无法打开。
- 完成国内外访问、浏览器、出口 IP、DNS 和连续启停真机验收。

## v0.1.24 — TUN 助手安全加固（2026-07-23）

- 助手只允许启停应用内置的 sing-box，并校验客户端身份、安装路径、签名指纹、内核指纹和配置范围。
- 使用只读文件描述符传递配置，并对失败默认拒绝、短读、文件描述符泄漏和身份竞态进行加固。

## v0.1.23 — 网络观测与路由控制（2026-07-22）

- 新增出口 IP 和 DNS 诊断、连接实时速率、节点地区和倍率。
- 新增测速并自动选择最快节点、按应用进程分流、配置备份与恢复。

## v0.1.22 — 固定侧栏控制（2026-07-22）

- 将唯一的原生侧栏按钮固定在工具栏左侧，补齐真实界面验收。

## v0.1.21 — 精简侧栏交互（2026-07-22）

- 移除自定义重复按钮，只保留 macOS 原生侧栏控制。

## v0.1.20 — 核心加固与界面增强（2026-07-22）

- 并入存储权限、诊断脱敏、并发测速、规则集下载和日志读取等安全与性能改进。
- 新增搜索、排序、侧栏折叠和仪表盘控制卡。

## v0.1.19 — 可日常使用的首个公开版（2026-07-21）

- 以订阅配置为中心，支持机场策略组、多协议节点和 Shadowsocks simple-obfs。
- 节点切换即时生效，增加连接监控页，并提供可拖入“应用程序”的 DMG。

## 中间开发版本

下列版本用于本机验证或发布候选，未作为对外 GitHub Release 保留：

- **v0.1.62**：补强运行恢复、网络身份重探、诊断导出与资源门禁。
- **v0.1.74~0.1.77**：菜单栏迷你面板与界面轮次，仅本机试装；其中 v0.1.77 曾短暂发布，
  相关功能与修复随 v0.1.79 重新发布。
- **v0.1.78**：运行期自诊断（CPU 异常与 DNS 停摆留证），仅本机安装，未发布 GitHub。
- **v0.1.68**：加入强制代理域名、IP 和 CIDR，后续随 v0.1.72 发布。
- **v0.1.70**：完成轻量交互、可取消测速、规则诊断和发布脚本；真机验收发现端口切换风险后阻止发布。
- **v0.1.71**：将内核端口移出临时端口池，但未完整解决 root/user 端口交接，最终改为 v0.1.72 的 relay 架构。

v0.1.19 之前为快速原型期，主要完成原生界面、sing-box 集成、系统代理、TUN、配置生成、测速和基础恢复链路。
