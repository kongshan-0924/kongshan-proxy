# 版本历史

空山的 GitHub Releases 只保留最新安装包，旧版本的用户可见变化记录在这里。以下内容经过归纳，不等同于逐条提交日志。

## v0.1.80 — 资源纪律修复（2026-08-21）

- 修复自诊断采样器的 mach port 引用泄漏：`task_threads` 返回的线程 send right 逐一归还，
  避免约 11 天后引用计数溢出使线程数指标静默失效，以及线程池短命线程留下死名字。
- 删除订阅时一并删除其缓存 YAML（内含节点凭据）；启动时兜底清理不再对应任何订阅的
  孤儿缓存（真机曾积累 5 个共 27 MB）。
- 深度资源排查结论：运行中堆泄漏仅 14 KB 且全部来自系统框架；FD、端口、线程、RSS 均无
  增长趋势；relay、日志存储、API 流的释放路径逐一复核通过。

## v0.1.79 — 修复后台 CPU 燃烧（2026-08-20）

- 修复菜单栏迷你面板关闭后 SwiftUI 宿主继续常驻观察应用状态的问题：面板改为开时新建、
  关时释放。此前开过一次面板后，速率每秒的变化会驱动不可见视图持续求值。
- 移除仪表盘与迷你面板里挂在每秒变化数值上的弹簧动画：动画在下一次采样前无法收敛，
  会按屏幕刷新率持续插值字形。真机代价为平均 57% CPU 连烧 8 小时、峰值内存 602 MB。
- v0.1.78 引入的运行期自诊断捕获并定位了上述问题；本版同时把它的 CPU 告警改为指数退避，
  修正活跃连接数读数。
- 并入 v0.1.74–0.1.77 的界面轮次：菜单栏迷你仪表盘、仪表盘主区重做、紧凑信息密度、
  自适应布局、规则页分区折叠。

## v0.1.73 — SSH 指定目标走代理（2026-08-14）

- 规则页新增“SSH 走代理”：按精确 IP + TCP 端口保存目标，OpenSSH 通过 ProxyCommand 经当前节点连接；支持 IPv4/IPv6、同 IP 多端口，同 IP 的其他端口不受影响。
- 只管理 `~/.ssh/config` 顶部带标记的 Include 和独立托管文件，原子写入、0600/0700 权限，符号链接与异常标记一律拒绝修改；不读取用户名、密码或私钥。
- 代理关闭时规则只保存在本机，开启后自动生效；停止、退出或启动失败时自动撤下，不留指向失效端口的配置。
- 修复仪表盘监控测试在发布门禁单独重跑时的偶发失败。

## v0.1.72 — 稳定入口与轻量交互（2026-08-09）

- 新增 App 用户态 TCP relay，系统代理始终连接稳定的本地端口；系统代理与 TUN 往返时只切换内核后端，避免端口争抢和短暂断网。
- 新增强制代理域名、IP 和 CIDR，并补充规则命中测试、连接出站链路与快捷规则操作。
- 批量测速改为有界并发、分批刷新并支持取消；配置、规则和节点切换减少不必要的内核重启。
- 仪表盘、日志和实时数据改为节流、合并发布与按可见性运行，菜单栏保留整机网速但不再周期重建展开的菜单。
- 补强发布门禁、安全替换、配置备份和系统代理/DNS 恢复验证。

## v0.1.67 — 原生菜单与代理页性能（2026-08-04）

- 状态栏改用 AppKit 原生菜单，保留实时速度，修复节点子菜单闪烁、跳动和“打开面板”失效。
- 修复策略组图标、右上角按钮被截断以及无订阅规则时的规则页显示。
- 节点测速、配置切换和代理开关脱离主线程重型工作，降低整页重绘和无界并发带来的卡顿。
- URL Test 任务与页面生命周期绑定，离开页面后及时停止不再需要的刷新。

## v0.1.59 — TUN IPv6 兼容（2026-07-30）

- 修复将 ULA 内网 IPv6 误判为公网 IPv6 的问题，解决 TUN 下部分应用上传失败。

## v0.1.58 — 单实例保护（2026-07-30）

- 阻止多个空山副本同时接管系统代理，避免竞态修改系统网络设置。

## v0.1.57 — 菜单栏整机网速（2026-07-30）

- 状态栏速度改为读取整机物理网卡，代理关闭时也可显示，并避免重复统计 TUN 流量。
- 调整代理页和设置页的布局、说明与操作反馈。

## v0.1.54 — 全流程稳定性走查（2026-07-30）

- 完成从启动、导入、切换、测速到退出恢复的真机走查，集中修复 11 处交互、状态同步和异常路径问题。

## v0.1.52 — 成品化打磨（2026-07-30）

- 更新应用图标，增加会话流量展示和多链接批量导入。
- 改善日志、规则和连接页的信息密度，减少重复与噪声。

## v0.1.51 — 内网 DNS 分流（2026-07-30）

- TUN 启动时探测局域网 DNS，为企业内网、家庭设备和反向解析生成独立路由。
- 节点域名解析改用无连接 UDP，避免长连接 DoH 失效导致的周期性停顿。

## v0.1.45 — 系统代理端口稳定（2026-07-28）

- 优先复用持久化的 mixed 端口，减少内核重启时系统代理反复重连。
- 这一阶段的端口复用仍受 root/user 绑定竞争影响，最终由 v0.1.72 的稳定 relay 架构完整解决。

## v0.1.43 — 免密码 TUN 全链路（2026-07-28）

- 打通特权助手的安装、启停、状态检查和异常恢复，日常使用 TUN 不再重复输入密码。
- 增加换网、睡眠唤醒、内核崩溃和孤儿进程恢复。

## v0.1.30 — TUN 真机网络修复（2026-07-23）

- TUN 固定使用 gVisor + Fake-IP，系统 DNS 指向 TUN 接口自身，解决开启 TUN 后网页无法打开。
- 完成国内外访问、浏览器、出口 IP、DNS 和连续启停真机验收。

## v0.1.24 — TUN 助手安全加固（2026-07-23）

- 助手只允许启停应用内置的 sing-box，并校验客户端身份、安装路径、签名指纹、内核指纹和配置范围。
- 使用只读文件描述符传递配置，并对失败默认拒绝、短读、文件描述符泄漏和身份竞态进行加固。

## v0.1.23 — 网络观测与路由控制（2026-07-22）

- 新增出口 IP 和 DNS 诊断、连接实时速率、节点地区和倍率。
- 新增测速并自动选择最快节点、按应用进程分流、配置备份与恢复。

## v0.1.22 — 固定侧栏控制（2026-07-22）

- 将唯一的原生侧栏按钮固定在工具栏左侧，补齐真实界面验收。

## v0.1.21 — 精简侧栏交互（2026-07-22）

- 移除自定义重复按钮，只保留 macOS 原生侧栏控制。

## v0.1.20 — 核心加固与界面增强（2026-07-22）

- 并入存储权限、诊断脱敏、并发测速、规则集下载和日志读取等安全与性能改进。
- 新增搜索、排序、侧栏折叠和仪表盘控制卡。

## v0.1.19 — 可日常使用的首个公开版（2026-07-21）

- 以订阅配置为中心，支持机场策略组、多协议节点和 Shadowsocks simple-obfs。
- 节点切换即时生效，增加连接监控页，并提供可拖入“应用程序”的 DMG。

## 中间开发版本

下列版本用于本机验证或发布候选，未作为对外 GitHub Release 保留：

- **v0.1.62**：补强运行恢复、网络身份重探、诊断导出与资源门禁。
- **v0.1.74~0.1.77**：菜单栏迷你面板与界面轮次，仅本机试装；其中 v0.1.77 曾短暂发布，
  相关功能与修复随 v0.1.79 重新发布。
- **v0.1.78**：运行期自诊断（CPU 异常与 DNS 停摆留证），仅本机安装，未发布 GitHub。
- **v0.1.68**：加入强制代理域名、IP 和 CIDR，后续随 v0.1.72 发布。
- **v0.1.70**：完成轻量交互、可取消测速、规则诊断和发布脚本；真机验收发现端口切换风险后阻止发布。
- **v0.1.71**：将内核端口移出临时端口池，但未完整解决 root/user 端口交接，最终改为 v0.1.72 的 relay 架构。

v0.1.19 之前为快速原型期，主要完成原生界面、sing-box 集成、系统代理、TUN、配置生成、测速和基础恢复链路。
