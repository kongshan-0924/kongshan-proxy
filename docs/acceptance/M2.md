# kongshan M2 验收记录

日期：2026-07-20  
功能基线提交：`6195d7e`  
产物：`dist/kongshan.app`（约 50 MB，ad-hoc 签名）

## 自动验收：通过

执行：

```bash
zsh scripts/verify_m2.sh
```

结果摘要：

- `swift test`：59/59 通过，0 failure。
- AppState 使用真实内置 sing-box 与 Clash API 完成快速重启；本轮实测 0.216799667 秒。
- `swift build -c release --arch arm64`：通过。
- App 主程序与内置 sing-box：均为 Mach-O arm64。
- `codesign --verify --deep --strict` 与 `plutil -lint`：通过。
- 内置 sing-box：1.13.14，darwin/arm64。
- 三个当前官方 `.srs` 均下载成功、非空，并由打包内核执行 `rule-set decompile` 成功。
- 含“自定义 → bypass → 私网 → 广告 → 中国 → final”的本地 binary rule-set fixture 通过打包内核 `check`。
- 脚本最终输出：`M2 automated verification passed`。

## 规则集来源与验证策略

- 中国域名：`SagerNet/sing-geosite` 的 `rule-set/geosite-cn.srs`。
- 中国 IP：`SagerNet/sing-geoip` 的 `rule-set/geoip-cn.srs`。
- 广告域名：`SagerNet/sing-geosite` 的 `rule-set/geosite-category-ads-all.srs`。
- 下载内容先写入临时文件，并由内置 sing-box 解析验证；只有成功后才原子替换正式缓存。
- HTTP 错误、空内容或解析失败不会覆盖缓存；旧缓存也必须再次解析成功才能兜底。
- 广告拦截关闭时不下载或加载广告规则集。

## 产物与可复现证据

- 主程序 SHA-256：`6d7dc9aa80ea9b3641945aec2cc3d950b173c3f6d8e3d13914c73564c9cae58c`。
- 内置 sing-box SHA-256：`813d8effd02a19572a8d75aef29fc073101404ca535b2496be86f21827c7684d`。
- `rules.json`、规则集缓存与诊断 `config.json` 均位于 `~/Library/Application Support/kongshan/`；Clash secret 仍只存在内存中。
- 自动系统代理测试全部使用命令记录器，没有调用真实 `networksetup`。

## 无网络变更界面冒烟

- 空节点 release App 正常启动，主线程处于 AppKit 事件循环空闲，约 15.5 MB。
- 规则页 debug/release 编译通过，侧栏、规则编辑器、两类 bypass 与广告开关均由原生 SwiftUI 组成。
- 冒烟前后无 `~/Library/Application Support/kongshan/proxy-recovery.json`。
- 本机 `LSUIElement + Bartender` 环境使 Computer Use 无法取得主窗口可访问性树；未为自动测试加入产品专用入口，视觉点击保留为人工验收。

## 未执行的人工验收

以下项目需要真实节点、浏览器流量、系统代理变更或可见菜单栏，本次未自主执行：

- 从菜单栏打开主窗口，检查“规则”页在浅色/深色模式下的布局、键盘操作、拖拽排序与错误提示。
- 使用真实节点访问中国站点，结合 sing-box route 日志确认 `geosite-cn/geoip-cn → direct`。
- 开启广告拦截后访问可复现广告域名，确认 `geosite-category-ads-all → reject`，且关闭后不再命中。
- 新增“进程名 → 直连”规则，用目标 macOS 进程发起连接并确认规则命中。
- 新增一个绕过域名，同时核对 route 最高优先级 direct（仅次于自定义规则）与真实 `networksetup -getproxybypassdomains` 两处均生效。
- 代理开启期间连续修改规则，使用持续请求测量真实网络中断时间小于 2 秒；自动集成测得 0.216799667 秒不能代替真实浏览流量测量。
- 断网启动并确认三个规则集使用最后成功缓存，同时在界面展示 warning。
- 真实代理开启后制造新配置失败与 bypass 更新失败，核对旧配置、旧 bypass 和最终系统代理恢复行为。

TUN 的第三处 bypass 注入（`route_exclude_address`）、DNS 泄漏与权限流程属于 M3，不在 M2 自动通过结论内。

## 复现命令

```bash
cd /Users/kaysen/workspace/mac/代理软件
zsh scripts/verify_m2.sh
open dist/kongshan.app
```
