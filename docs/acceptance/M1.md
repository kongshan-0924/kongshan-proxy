# kongshan M1 验收记录

日期：2026-07-20  
基线提交：`c8f5f9868fab69e2d169b59436bd98e7fa81f779`  
产物：`dist/kongshan.app`（约 50 MB，ad-hoc 签名）

## 自动验收：通过

执行：

```bash
zsh scripts/verify_m1.sh
```

结果摘要：

- `swift test`：34/34 通过，0 failure。
- `swift build -c release --arch arm64`：通过。
- App 主程序与内置 sing-box：均为 Mach-O arm64。
- `codesign --verify --deep --strict`：通过。
- `plutil -lint`：通过。
- 内置 `sing-box version`：1.13.14，darwin/arm64，含 `with_clash_api`。
- 内置 `sing-box check -c /dev/stdin`：最小配置通过。
- 脚本最终输出：`M1 automated verification passed`。

## 内核来源与完整性

- 来源：sing-box 官方 GitHub Release `v1.13.14` 的 `darwin-arm64` 归档。
- 归档 SHA-256：`73e8967b0fc08e17bce4263ca56ebc394822401a16497a1c4e02316c888202ab`。
- 解包后内核 SHA-256：`813d8effd02a19572a8d75aef29fc073101404ca535b2496be86f21827c7684d`。
- Vendor 与 App 内置内核 SHA-256 一致。
- 主程序 SHA-256：`d9594a8bf62ed0266f397b2a610b18202aa49b24aa79a99d15221e4f064e7a92`。

## 无网络变更冒烟

- 通过打包后 `.app` 启动，进程、`com.kaysen.kongshan` LaunchServices bundle 与系统状态项均成功注册。
- 主线程采样显示 App 正在 AppKit 事件循环空闲，约 15 MB、0% CPU。
- 空节点 AppState 测试确认开启操作不会执行任何 `networksetup` 命令。
- 冒烟前后均无 `~/Library/Application Support/kongshan/proxy-recovery.json`。
- 本机菜单栏管理环境把新状态项放到不可见坐标，自动界面工具无法展开菜单和点击“打开 kongshan”；这两项保留为人工界面验收。

## 未执行的人工验收

以下操作会使用真实订阅、节点或修改当前 Mac 网络代理，本次未自主执行：

- 在本机菜单栏显示 kongshan 状态项，展开菜单并打开主窗口。
- 导入用户的真实 Clash 订阅，核对节点数量、分组、协议标签、切换和单个/批量延迟。
- 添加真实自建 Hysteria2 节点并验证连通。
- 开启系统代理，用浏览器访问 Google，并确认 HTTP/HTTPS/SOCKS 均指向当次 mixed 端口。
- 关闭与正常退出 App，逐个核对所有启用网络服务的 HTTP/HTTPS/SOCKS/bypass 原值已精确恢复。
- 开启代理后强制杀死 App，重启后核对 `proxy-recovery.json` 自愈恢复并被删除。
- 用真实环境记录无节点/坏订阅/断网时的界面错误和旧缓存兜底表现。

TUN、完整分流、DNS 泄漏、24 小时资源趋势等不属于 M1 交付范围，按 M2–M4 里程碑继续。

## 复现命令

```bash
cd /Users/kaysen/workspace/mac/代理软件
zsh scripts/verify_m1.sh
open dist/kongshan.app
```
