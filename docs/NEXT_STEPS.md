# 下一步

## 2026-08-23 v0.1.82 之后（DNS 引导解析器已落地）

1. **观察 DNS 解耦效果（P1）**：v0.1.82 安装后，引导解析器默认仍跟随国内 DoH（隐私意图保留）；
   若真实使用中仍偶发「节点域名解析」超时，可在 设置 → DNS 高级设置 填入独立引导解析器 IP
   （如 `114.114.114.114`）与国内 DoH 解耦。观察一段时间后回调是否值得默认解耦。
2. **P1 · 节点失败率阈值回调**：10 分钟窗口、≥20 尝试、≥5 失败、≥10% 为首版估计值，
   观察真实使用后再调。
3. **P1 · 旧 Release 清理（需用户）**：`gh release delete v0.1.80` 等命令被环境权限分类器拦截，
   需用户自行执行（存档元数据见 HANDOFF/SESSION_LOG）。
4. **发布收敛**：远端只保留最新 Release；本机备份只留最近 3 份。

## 2026-08-21 v0.1.80 之后

1. **观察自诊断（P1）**：几天内消息页不应再出现「CPU 占用持续偏高」；若出现，
   mainThreadShare 等字段直接给方向。首启应出现一条「已清理订阅缓存孤儿」事件（5 个）。
2. **TUN 助手重装（P1，需用户）**：cdhash 已变，首次用 TUN 前在设置 → 隧道重装一次。
3. **发布收敛**：远端只保留最新 Release；本机备份只留最近 3 份。


## 2026-08-18 当前待办（按优先级）

1. **P1 · 用户裁决 DNS 上游取舍。** `dns-bootstrap` 与 `dns-cn` 都指向 `223.5.5.5`，
   一台抖动会同时打掉节点域名与国内域名解析。sing-box 1.13 无故障转移能力（已逐字段验证），
   只能在选址上解耦，但解耦会重新引入 `DNSConfigTests.swift:72` 刻意避免的隐私问题。
   三个选项：保持现状（接受偶发 10 秒停摆）／解耦（放弃“引导跟随用户选择”）／
   新增可配置的引导解析器字段（多一处设置项）。**改动已回滚，等决定。**

2. **[已完成] v0.1.79 已发布并安装。** 全量 472/1/0，M4 平均 0.840%，远端 digest 一致。
   观察几天确认修复后：清理 v0.1.77 与 v0.1.73 的 Release/标签（只保留最新）；
   TUN 助手因 cdhash 变化需在设置 → 隧道重装一次。
   （历史记录：）**v0.1.78 候选已就绪，待 publish 与安装。** 自诊断已升版到 v0.1.78 并走完
   `release.sh prepare`（468 执行 / 1 跳过 / 0 失败，M4 平均 CPU 0.820%），
   同时把 v0.1.74–0.1.77 四个未过门禁的界面提交纳入了完整验收。
   **已安装 v0.1.78/build 178**，自诊断已在线开始留证。
   只剩 `scripts/release.sh publish`（被环境权限分类器拦截，需用户自行运行）。
   App cdhash 已变，首次用 TUN 前需在设置 → 隧道重装一次助手。

3. **[已解决 → 转观察] CPU 燃烧已定位并修复（v0.1.79）。** 根因是 popover hosting
   controller 常驻观察 + 高频值弹簧动画（详见 HANDOFF 当前状态）。上轮「后台线程」的
   推断有误——`ps -M` 在本进程枚举不全，`thread_info` 实测主线程占 99%。
   接下来只需观察：v0.1.79 运行几天后消息页不应再出现「CPU 占用持续偏高」；
   若再出现，事件里的 mainThreadShare 与新修正的活跃连接数会给出下一步方向。
   （原文保留：）**定位后台线程上的突发重计算。** CPU 异常在 2026-08-18 当天复发：清醒期平均约 29%，
   且**代理是关闭的**。已当场排除两项——不是窗口可见导致的空转（关 0.53% / 开 0.67%），
   也不是 SwiftUI/主线程（进程 1070 分钟，主线程仅 8.5 秒）。范围已缩到**已被回收的后台
   并发/派发工作线程**，user:system ≈ 16:1 说明是纯计算。
   下次复现时消息页会出现「CPU 占用持续偏高」事件，其中 `mainThreadShare`、user 占比、
   窗口可见性、活跃连接数、日志流入行数合起来应能定位到子系统。
   捕获后按真实数据回调阈值（当前 15 秒采样、12%、连续 3 次、10 分钟中途报告，均为估计值）。

4. **P1 · v0.1.77 补跑 M4，或先回滚到 v0.1.73 做对照。** v0.1.74–0.1.77 都是 trial build，
   从未走过门禁；CPU 异常正是在 v0.1.77 上观测到的，有可能就是这四个界面提交引入的。
   **在此之前不要删除 v0.1.73 的 Release 与标签**——它是最后一个经过完整验收的版本，
   既是回滚点也是对照组。

5. **P2 · 界面四版本的真机走查。** 0.1.74–0.1.77 改了七个视图且无走查记录；
   菜单栏 popover 与自适应布局属于历史上反复出问题的区域（见「别改回去的设计点」第 5 条）。

6. **P3 · 磁盘（本轮已处理）。** 停用的 `sing-box-tun.log` 与最旧两个配置备份已移入废纸篓，
   logs 24MB → 9.0MB、备份 14MB → 9.2MB。当前日志轮转有上限（5MB × 2），正常。

## 长期未结（需用户参与，沿用既有结论）

- 睡眠唤醒后隧道自动重建、真实换网恢复、无透明代理网络下的出口 IP 验证。
- 想彻底消掉「App 更新后要重装助手」只有两条路，都需要付费 Apple Developer 账号：
  改用 NetworkExtension，或用 Developer ID 把认人方式换成签名 requirement。
  **不要**为省这一次弹窗放宽 cdhash 校验。详见 `docs/design/tun-authorization-approaches.md`。

## 长时间资源门禁

```sh
KONGSHAN_HEALTH_SAMPLE_COUNT=360 KONGSHAN_HEALTH_INTERVAL_SECONDS=10 scripts/verify_long_run_health.sh
```

脚本不启动、不停止、不重启 App，也不修改系统代理；超出门槛时把诊断保存在 `/private/tmp`。

## 暂不扩张

- 不加 TUIC / WireGuard，除非真实订阅出现需求。
- 不引入 Sparkle；private 仓库没有可安全匿名访问的更新源。
- 不为拆文件而重写 `AppState`；只随真实功能按领域拆 extension。

---


## 2026-08-14 v0.1.73 发布收口

v0.1.73 已提交、通过 prepare 门禁并发布到 GitHub（main 与标签已推送，远端 digest 与本地
SHA-256 一致，v0.1.72 Release/标签已清理）。**尚未安装到本机**：需要部署时运行
`scripts/release.sh install`，它会正常退出旧版、确认系统代理/DNS/TUN 和网络恢复、备份配置后
原子替换。安装后在规则页添加 `118.69.52.186`、端口 `22235`，开启代理，再用
`ssh -p 22235 gary@118.69.52.186` 做真实链路和延迟验收。App 更新后 cdhash 变化，
首次使用 TUN 需在设置 → 隧道重装一次助手。

## 2026-08-13 SSH 指定目标功能部署（已完成源码与发布）

源码和自动验证已完成。下一步若要交付到当前 Mac：先提交改动并运行发布门禁，备份配置；只让旧版正常
退出，确认系统代理、DNS、TUN 和网络已恢复后再原子安装新版。打开新版后在规则页添加
`118.69.52.186`、端口 `22235`，开启代理，再用 `ssh -p 22235 gary@118.69.52.186` 做真实链路和延迟
验收。部署前不手工修改真实 `~/.ssh/config`，也不在代理运行时强制替换 App。

## 2026-08-09 v0.1.72 发布收口

v0.1.72 的代码、M4、正常退出与网络恢复、配置备份、原子安装、helper 授权和系统代理/TUN 真机矩阵均已
完成。公开端口在所有模式往返中固定为 `36815`，最终已恢复“奶昔”/`Japan 03`/仅系统代理，数据面正常。

当前只剩发布事务：提交本轮验收记录，重新运行 `scripts/release.sh prepare` 生成与最终提交绑定的唯一 DMG；
确认候选 App CDHash 与安装版一致；运行 `publish` 推送 main/v0.1.72 并创建 GitHub Release；核对远端
DMG digest 与本地 SHA-256；删除 v0.1.67 Release/标签；最后确认 `dist` 只保留 v0.1.72、Git 工作区
干净、安装版仍保持系统代理与最终配置。发布过程中不需要再次退出或替换当前 App。

发布后的非阻塞观察项：真实配置下 App RSS 稳定在约 154~157 MB，低流量 CPU 存在 1%~4% 间歇峰值；
10 秒调用栈未发现 relay 自旋或持续忙循环。只有长时间样本出现 RSS 单向增长、平均 CPU 持续超过 5%，
或电池影响明显时，再单独为状态栏绘制/常驻 Clash 推流做 Instruments 定量优化。

## 2026-08-07 v0.1.68 本地部署边界

规则页“强制代理”已完成并本地安装为 v0.1.68/build 168；支持域名、单 IP 与 CIDR，处理系统代理
bypass/TUN exclude 冲突，且仅在规则模式改变绕过项。全量测试、M4、签名、DMG、配置备份、安全替换、
界面和空闲资源终验均通过。`dist` 只保留 v0.1.68 DMG，GitHub 尚未推送或发布。

当前唯一部署后事项是免密码助手显示“需重装”：这是 App 更新后 cdhash 变化的安全设计结果。需要用户明确
确认后，在设置 → 隧道点一次“重新安装”并输入管理员密码，再真机验证 TUN 零弹窗启停。发布 GitHub 也
留待用户明确要求；发布前应再确认 Git 状态、提交、标签和 Release 资产 digest。

## 历史：v0.1.67 发布收口

v0.1.67 已完成配置备份、完整 M4、单副本原子安装、只保留最新 DMG、代码和标签推送、GitHub Release
及旧 Release/旧版本标签清理。Release 资产 digest 与本地 SHA-256 一致。
助手真机身份探针确认 `isTrusted = true`，无需重装；系统中只保留 `/Applications/kongshan.app`，程序坞
也只有这一项。后续构建 App 位于隐藏且忽略的 `.build`，`dist` 只输出最新 DMG。

发布后实测中 v0.1.67 正以系统代理模式健康运行：App 平均 CPU 0.190%、峰值 0.9%、最大 RSS 62,400 KB，
App/core FD 最大 85/136、CLOSE_WAIT 0；经代理访问 GitHub 返回 200。四个订阅自动更新当前都已关闭，
先前 TAG HTTP 401/TLS 告警不会再触发每 15 分钟重试，因此不再是待办。

## 历史：v0.1.66 当前无阻塞项

当前没有构建、运行或性能阻塞项。v0.1.66 已完成全量测试、构建、验签、DMG 校验、
M4 门禁和安全替换；新版系统代理保持关闭，无内核或恢复残留，直连网络正常。

代理页图标、测速按钮布局、高频视图计算和批量测速均已优化。141 节点 URL 测速实测约
17.3~26.1 秒（受节点/网络超时影响），不再逐节点刷新整页；退出测速内核残留问题也已修复。

状态栏已迁到 AppKit `NSStatusItem + NSMenu` 并恢复 2 秒整机速度刷新；速度只更新固定宽度按钮图片，
持久化菜单不随之重建。自动化已覆盖菜单对象身份和原生动作，但状态项不在应用窗口 AX 树中，仍请用户
手动确认速度持续变化、节点子菜单保持 10 秒、节点可选择、关闭主窗口后“打开仪表盘”有效。

v0.1.61 在 21:44~21:50 的历史基线再次取样仍通过：安静期 App 平均 CPU 3.76%、峰值 5.7%、RSS 184 MB；
连接突发后 FD 与 RSS 均能回落，无 CLOSE_WAIT、无新崩溃或持续错误簇，不需要继续改性能代码。

## 运行优化优先级

1. **P1：菜单鼠标终验。** 观察速度变化，展开任一节点子菜单保持 10 秒并移动选择；关闭主窗口后从
   状态栏点击“打开仪表盘”。若均正常，本次菜单问题即可关闭。
2. **P1：真实换网验收。** 在 TUN 或系统代理运行中切换公司网、家庭网和手机热点，确认
   v0.1.67 能重探 LAN DNS、刷新 DoH 传输并自动恢复数据面。
3. **P1：睡眠唤醒验收。** 在 TUN 运行时完成一次合盖休眠和唤醒，确认隧道自动重建、网页恢复，且
   关闭代理时系统网络能正常还原。
4. **P2：继续观察 URL 全量测速。** 当前 16 并发、3 秒超时是响应速度与瞬时负载的平衡；只有真实使用
   仍经常超过 30 秒或出现明显输入卡顿时，再考虑可取消任务或按策略组测速，不继续盲目抬高并发。
5. **P2：继续观察极端日志量。** v0.1.61 未聚合日志突发是最高负载（平均 6.36%、峰值 15.9%），
   已通过门禁；只有真实使用中接近 2,000 行并持续高频流入时才需要继续优化。
6. **P2：旧日志清理。** 当前日志共约 26 MB，其中 `sing-box-tun.log` 15.7 MB 且已停用，现有
   “清理可再生缓存”会覆盖它；是否清理由用户在 UI 中决定，不自动删除。
7. **P2：TUN 助手。** App 更新会改变 ad-hoc cdhash；下次使用 TUN 若界面提示助手需重装，按 UI
   正常重装一次，绝不能放宽 helper 的 cdhash 校验。
8. **观察项：重复配置应用。** 16:34 出现三次连续成功应用和两条 `context canceled`；现有证据符合
   连续重载取消旧连接。只有确认“单次点击也触发多次重载”时，才立项做按钮防抖或任务合并。

## 已完成、不再列为待办

- 未生效订阅/节点变化不再重启当前内核；必须重建时会记录重载前后 PID、失败和回滚事件。
- 真实网络身份变化会通过 SystemConfiguration 获取物理接口，再从 DHCP 重探 LAN DNS；
  同时重建内核刷新 DoH 传输，避免继续使用旧网络上的连接。
- 消息页已提供最多 200 条、持久化且脱敏的运行事件，换网、睡眠唤醒和自动恢复可追溯。

## 长时间资源门禁

日常无需常驻额外监控。需要 1 小时只读采样时运行：

```sh
KONGSHAN_HEALTH_SAMPLE_COUNT=360 KONGSHAN_HEALTH_INTERVAL_SECONDS=10 scripts/verify_long_run_health.sh
```

脚本不启动、不停止、不重启 App，也不修改系统代理；超出 CPU/RSS/FD/CLOSE_WAIT 门槛时
会把诊断保存在 `/private/tmp`。

## 需要用户参与才能推进

- **睡眠唤醒的隧道自动重建**：合盖休眠 → 唤醒，看是否自动恢复；没恢复时关掉再开也应正常。
- **真实换网恢复**：代理运行中切公司网 / 家庭网 / 手机热点，核对运行事件、内网域名和外网访问。
- **经代理的出口 IP**：需换一个没有透明代理的网络（手机热点最省事）。当前家庭网络在做
  透明代理 + SNI 分流，判据见 `docs/design/tun-real-machine-debug.md`。

## 想彻底消掉「App 更新后要重装助手」

只有两条路，都需要付费 Apple Developer 账号：改用 NetworkExtension（Surge/Stash 的做法），
或用 Developer ID 签名把助手的认人方式换成签名 requirement（ClashX 的做法）。
**不要**为省这一次弹窗放宽 cdhash 校验——ad-hoc 的 identifier 谁都能伪造。
详见 `docs/design/tun-authorization-approaches.md`。

## 暂不扩张

- 不加 TUIC / WireGuard，除非真实订阅出现需求（内核已支持，缺的只是转换分支）。
- 不引入 Sparkle；private 仓库没有可安全匿名访问的更新源。
- 不为拆文件而重写 `AppState`；只随真实功能按领域拆 extension。
