# 下一步

1. 执行 M4 Task 1：按 TDD 实现 `DNSSettings` 与纯双 DoH 配置，先观察缺少 API 的 RED。
2. 使用打包 sing-box 1.13.14 对 system/TUN、默认/自定义 DNS 和域名节点执行真实 `check`。
3. Task 1 完成后立即记录并提交，再进入 DNS 在线事务与设置 UI。
4. 真实管理员授权/TUN 与 M1/M2 网络验收继续按三份 acceptance 文档保留。
