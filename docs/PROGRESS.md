# 项目进度

## 2026-08-23 v0.1.82 DNS 引导解析器可配置 + 旧 Release 清理（已提交，待门禁与部署）

- [x] 用户裁决：新增可配置的引导解析器字段，而非直接解耦或保持现状。
- [x] `DNSSettings.bootstrapResolver`：留空 = 跟随国内 DoH 的 IP（隐私意图保留）；
  填入 IPv4/IPv6 = dns-bootstrap 使用该独立上游，与国内 DoH 解耦，一台上游抖动
  不再同时打掉「节点域名解析」与「国内域名解析」。
- [x] 校验：只接受 IP（域名/URL/带端口拒绝），旧版 settings.json 缺字段兼容解码。
- [x] 设置 → DNS 高级设置新增「引导解析器（可选）」输入与说明。
- [x] 测试：解耦生成、校验、旧配置解码、编码 round-trip、system/TUN × 自定义引导的
  bundled core check，新增 4 条；全量 493/2 跳过/0 失败（跳过均为环境相关）。
- [x] 已提交 `60ac192`（8 文件 +200/-6）；`swift build -c release` 通过。
- [x] 旧 Release 清理完成（用户确认）：v0.1.80/79/77/73 的 Release 与标签已删除，
  远端只保留 v0.1.81（Latest）与基线标签。
- [ ] 尚未推送 main、构建 DMG 或安装；部署前按既有 release 门禁备份配置并正常退出旧版。
- [ ] 安装后观察：默认跟随国内 DoH 的行为不变，解耦入口可用即达成目标。

## 2026-08-23 v0.1.81 可观测性补强（已发布并安装）

- [x] v0.1.80 两天验证：生命周期平均 CPU 0.64%、RSS 无增长、端口零增长、0 崩溃、
  DNS 超时 41→2、自诊断仅 1 条短爆发告警。
- [x] 13 处失败事件补齐原因；`stop()` 记录触发方；`CrashRestartLimiter` 暴露自愈次数；
  源码守卫禁止再出现无 detail 的失败事件。
- [x] 新增 `OutboundFailureDetector` 并接入消息页，排除规则拒绝与重载取消两类误报，
  原因归一化后不残留任何服务器地址。
- [x] 消息页运行事件「只看问题」筛选；内核停止时重置两个检测器。
- [x] 全量 489/1/0；M4 平均 0.160%；已发布并安装 v0.1.81/build 181，空闲 0.37%。
- [ ] 节点失败率阈值为首版估计值，待真实使用回调。
- [ ] 旧 Release 清理待用户指示（v0.1.80 已满足"真实使用确认"条件）。


## 2026-08-21 v0.1.80 深度资源排查与修复（待发布）

- [x] 实测排查：`leaks` 14.4 KB（全系统框架、不累积）、FD 67、端口 267、线程 5、
  RSS 64 MB idle；v0.1.79 装后零 CPU 告警，弹簧动画修复初步生效。
- [x] 修复 `task_threads` 线程 right 泄漏（urefs 每天 +5,760，约 11 天溢出）；
  回归测试以调用线程端口 urefs 为判据，反向验证确切失败。
- [x] 修复删除订阅不删缓存：删除路径连带删 + 启动孤儿清理；真机现存 5 孤儿 27.2 MB
  将在新版首启自动清除。测试 4 条。
- [x] KernelLogStore、ClashAPIClient、LocalTCPRelay、ObserverBag 释放路径复核，无问题。
- [x] CHANGELOG 补齐 0.1.74~0.1.80。
- [x] 全量 477 执行 / 1 跳过 / 0 失败。
- [x] prepare 通过（M4 平均 0.120%）、已安装 v0.1.80/build 180、已发布 GitHub 并标记 Latest。
- [x] 孤儿清理端到端验证：首启移除 5 个，`subscriptions` 28 MB → 932 KB，事件已记录。
- [x] 本机收敛：单实例、单副本、dist 只留最新、备份 8→3 份；用户数据 54 MB → 12 MB。
- [ ] 旧 Release/标签（v0.1.73/v0.1.77/v0.1.79）待修复经数日真实使用确认后再清理。


## 2026-08-20 v0.1.79 CPU 燃烧修复与自诊断优化（待门禁与部署）

- [x] 自诊断在真机捕获完整异常：182 条事件 + 现场采样栈，定位到 popover hosting
  controller 常驻观察 + 四处高频值弹簧动画；一段爆发 8.25 小时 / 4.7 核·小时 / 峰值 103.5%。
- [x] 修复 popover 生命周期：开时新建、关时按对象身份释放，覆盖快速重开与 show 未成功两个边界。
- [x] 摘掉 DashboardView 三处与 MenuBarPopoverView 一处的
  `.contentTransition(.numericText()) + .animation(.smooth)`；低频动画保留。
- [x] CPU 中途报告指数退避（10 分钟起步、2 倍递增、封顶 2 小时）；诊断详情改读
  `activeConnectionCount`。
- [x] 新增 4 条回归（popover 释放、过期回调、源码守卫、退避），定向 27/27 通过。
- [x] 更正上轮两处错误结论（时间戳时区、`ps -M` 线程枚举不全），已记 SESSION_LOG。
- [ ] 全量测试、M4、prepare、安装、发布。


## 2026-08-18 运行诊断与运行期自诊断（源码完成，待门禁与部署）

- [x] 只读运行诊断：v0.1.77/build 177 已连续运行 3 天 13 小时；近 7 天 0 崩溃，
  App/core FD 247/208、CLOSE_WAIT 均为 0，运行事件 38 条全 info，无泄漏证据。
- [x] relay 架构真机复核通过：系统代理固定 `36815`（App 持有），内核内部 mixed `21338`。
- [x] 量化当前资源速率：空闲 1.66%（与 v0.1.72 的 1.745% 基线一致）、仪表盘可见 3.89%
  （门禁通过）、relay 转发 0.013 s/MB（与内核约 1:1）。
- [x] 定位到 DNS 结构性风险：`dns-bootstrap` 与 `dns-cn` 共享 `223.5.5.5`；当天 41 次
  10 秒解析超时中 7 次是节点自身域名（会让整条代理停摆），07:04~07:33 劣化约半小时。
- [x] 用 bundled 内核逐字段验证 sing-box 1.13 无 DNS 故障转移能力（`server` 不接受数组，
  `timeout`/`fallback` 均为 unknown field），确认只能在选址上解耦。
- [x] 新增 `ProcessResourceSampler` 与 `RuntimeAnomalyDetector`（CPU 异常段 + DNS 停摆聚合），
  接线到 `AppState`，15 秒采样、全程运行、退出前落盘，报告完全脱敏。
- [x] 新增 15 条定向测试（11 条检测器 + 3 条接线 + 1 条窗口语义回归），含 Mirror 递归隐私守卫。
- [ ] **待用户裁决**：是否解耦 dns-bootstrap 与 dns-cn。解耦会重新引入
      `DNSConfigTests.swift:72` 刻意避免的隐私问题（用户换掉阿里后节点域名仍被送去阿里）。
- [ ] 累计 CPU 18.2% 的成因仍未定论；等自诊断在真机捕获一次后再定位。
- [ ] 自诊断阈值为首版估计值，尚未被真实异常触发验证过。
- [x] 升版 v0.1.78；全量 468 执行 / 1 跳过 / 0 失败；M4 门禁通过（平均 CPU 0.820%、
  最大 RSS 109,120 KB）；`release.sh prepare` 生成提交绑定候选。
- [ ] publish 与安装待用户确认；安装后自诊断才开始留证。

## 2026-08-15 v0.1.74–0.1.77 界面轮次（事后补记）

**依据为 git 提交、安装版 Info.plist 与 GitHub Release 元数据**；当时未留会话记录，
改动动机无法还原，只按代码事实记录。

- [x] v0.1.74 `ce8d84b`：菜单栏迷你仪表盘 popover（新增 `MenuBarPopoverView.swift`）、仪表盘主区重做。
- [x] v0.1.75 `7436117`：紧凑信息密度与交互打磨，涉及七个视图与 `Theme`。
- [x] v0.1.76 `ba67c0c`：跟随窗口缩放的自适应布局，主要在 `LogsView`。
- [x] v0.1.77 `888ba44`：规则页分区可折叠，规则列表获得保底空间。
- [x] v0.1.77 已发布 GitHub 并安装为 `/Applications/kongshan.app`。
- [ ] 四次均为 “trial build” 版本号提交，**未走 `release.sh prepare` 的提交绑定门禁**，
      仓库中没有对应 M4 验收记录；界面改动也没有真机走查记录。


## 2026-08-13 指定 IP + SSH 端口走代理（源码完成，待部署）

- [x] 新增 `SSHProxyTarget` 与 `RoutingSettings.sshProxyTargets`，精确校验和去重 IPv4/IPv6 + 端口；
  旧 `rules.json` 无该字段时兼容为空。
- [x] 新增安全的 OpenSSH 托管配置：标记 Include、独立文件、原子写入、0600/0700 权限、符号链接与
  异常标记 fail-closed，清空最后一条规则时只移除空山托管内容。
- [x] sing-box mixed 入站和 `IP + port + tcp` 优先路由覆盖系统代理、纯 TUN、组合模式及直连出站模式；
  SSH 规则不影响同 IP 的其他端口和普通 TUN 排除语义。
- [x] AppState 完成保存、热重载、relay、启动/停止/退出/崩溃生命周期和失败回滚；修复 TUN 后置失败
  错用用户态回滚器导致 root/user 双内核并存的问题。
- [x] 规则页新增 IP、端口、添加/删除控件与本地隐私说明；关闭代理时只保存，开启后自动生效。
- [x] 最终定向回归 14/14；XCTest 449 项执行、1 跳过、0 失败；Debug 构建、差异格式、敏感模式扫描
  与 OpenSSH `ssh -G` 精确端口验收通过。
- [ ] 尚未提交、推送、构建 DMG 或安装；部署前按既有 release 门禁备份配置并正常退出旧版。

## 2026-08-09 v0.1.72 稳定用户态代理入口（已安装并完成真机验收）

- [x] 纠正 v0.1.71 的临时端口池结论：普通 TCP 结束状态不阻塞重绑；活跃 `BOUND/CLOSED` PCB 与
  XNU 的不同 UID 具体地址绑定顺序才是 root/user 内核端口交接无法可靠完成的原因。
- [x] 淘汰 `reuse_addr/SO_REUSEPORT` 方案：它不能绕过 XNU 跨 UID 拒绝，还允许同 UID 进程加入
  无鉴权监听组，扩大本机攻击面。
- [x] 实现 App 用户态稳定 TCP relay：公开端口持久化为 `proxyRelayPort`，sing-box 每代使用独立内部
  mixed 端口；模式切换只换后端，不释放公开 listener。
- [x] 补齐双向转发、half-close、背压、后端切换、空目标、停止重绑、原子竞态与 AppState 端口隔离测试。
- [x] XCTest 435 项执行、1 跳过、0 失败；最终 M4 平均 CPU 0.240%，最大 RSS 115,712 KB；
  v0.1.72/build 172、arm64、deep/strict、sing-box 1.13.14 与残留检查通过。
- [x] 发布门禁改为直接运行 XCTest bundle，消除 SwiftPM 包装进程收尸挂起；完整 M4 新鲜返回成功。
- [x] 候选提交为 `8e7a9b3`；提交绑定的首次 `prepare` 通过，DMG、arm64、签名和镜像校验有效。
- [x] v0.1.71 只通过正常 App Quit 退出；退出后 App/core、系统代理、TUN DNS 和 recovery 均清除，
  直连 HTTPS 200，再备份配置并原子安装 v0.1.72。未发送 TERM/KILL。
- [x] 配置备份为 `kongshan-config-0.1.72-20260809-000441.tar.gz`，SHA-256
  `c2d9c2098c8671aacde3f8b80a3a28af91b4736054a675b264735491df8d1d13`；设置、规则、订阅元数据和
  六份订阅 YAML 哈希保持一致。旧版回滚副本已移到废纸篓。
- [x] helper 第二次授权成功，UI 显示“已安装”；系统代理、系统代理+TUN、仅 TUN、多轮切回均通过。
  公开 relay 端口始终为 `36815`，sing-box 内部 mixed 每代变化；TUN 关闭后 DNS 恢复 `192.168.2.1`。
- [x] HTTP、SOCKS5、普通 TUN 数据面均返回 200；仅 TUN 时三类系统代理关闭且无 relay 后端，新连接
  立即拒绝。最终恢复“奶昔”/`Japan 03`/系统代理，HTTP/SOCKS5/直连均为 200。
- [x] 最终只读健康复核：普通日志最近 2,000 行无 issue，近 2 小时统一日志 0 error/fault，近 7 天
  无崩溃；唯一运行事件 error 仍是 8 月 3 日 00:44 的历史启动失败。
- [x] 真实运行 20 次/2 秒样本：App CPU 平均 1.745%、峰值 5.2%，RSS 154.3~156.0 MB；core CPU
  平均 0.050%、RSS 49.5 MB。期间约 8~9 组 relay 连接且流量增量为 0；10 秒调用栈采样未发现忙循环。
- [ ] 从本记录提交重新 `prepare`，推送 main/v0.1.72、创建唯一最新 Release，删除 v0.1.67 Release/标签。

## 2026-08-08 v0.1.70 轻量交互、规则诊断与发布门禁（候选已验证）

- [x] 仪表盘高频指标改为 2 秒合并发布、图表 4 秒一点；窗口不可见时暂停图表，状态栏 2 秒速度保留。
- [x] 全量测速增加进度与取消，取消后停止派发并保留结果；URL 临时内核完成/取消后自动停止。
- [x] 强制代理支持批量输入和单次重载；连续规则应用 latest-wins 合并，UI 明示重载会断开现有连接。
- [x] 新增规则命中测试器与连接页右键快捷规则/复制/完整链路，统一解析域名、IPv4、IPv6 与端口。
- [x] 新增 15 分钟诊断模式，临时 debug、持久化截止时间、到期自动恢复，继续使用脱敏导出。
- [x] `scripts/release.sh` 覆盖提交绑定验证、配置备份、正常退出、代理/DNS/直连检查、原子安装和发布；
  M2/M3 验收去除在线规则集下载，M4 候选使用严格限定的隔离 support 目录。
- [x] 完整 M4：431 项执行、1 跳过、0 失败；平均 CPU 0.220%，最大 RSS 123,360 KB，签名、arm64、
  sing-box 1.13.14、规则集和残留检查通过。临时目录安全边界定向测试 2/2 通过。
- [ ] 提交后运行 `scripts/release.sh prepare` 生成最终 v0.1.70 DMG 与提交绑定验证戳。
- [ ] 正常退出 v0.1.68，确认 App/core、系统代理、TUN DNS、recovery 与直连网络恢复后安装 v0.1.70。
- [ ] 实机验收配置保留、系统代理、菜单、测速取消、规则/连接快捷操作和真实资源，再发布 GitHub。

## 2026-08-07 v0.1.68 强制代理域名/IP（已本地部署，未发布 GitHub）

- [x] 规则页新增“强制代理”，支持域名、单个 IPv4/IPv6 和 CIDR；单 IP 自动规范化为 `/32` 或 `/128`，
  可查看和删除，重复目标采用 upsert。
- [x] 强制规则复用 `RoutingSettings.customRules` 并排在绕过、订阅规则、私有网段和中国大陆直连之前；
  切换配置后若原策略组不存在，生成器安全回落到当前主代理出站。
- [x] 规则模式下自动移除冲突的 macOS 系统代理 bypass 与 TUN `route_exclude_address`；直连/全局模式
  保留原绕过语义，回环目标始终拒绝强制代理。
- [x] 13 条定向回归、离屏规则页快照、Impeccable detector 与全量测试通过；最终全量为 422 通过、
  1 跳过、0 失败，包含 bundled sing-box check、代理/DNS 回滚和 TUN/helper 信任边界。
- [x] `VERSION` 已升至 0.1.68；全量 422 通过、1 跳过、0 失败，隔离 M4 平均 CPU 0.260%、最大
  RSS 122,896 KB。
- [x] v0.1.68/build 168 已安全替换到 `/Applications/kongshan.app`；旧版正常退出后确认无 App/core、
  系统代理、recovery，直连 HTTPS 200。用户设置、规则和订阅元数据哈希保持一致。
- [x] `dist` 只保留 `kongshan-0.1.68.dmg`；DMG verify、arm64、deep/strict、sing-box 1.13.14 通过；
  DMG SHA-256 为 `009fdf679a767a245491ed48280071d0d39f32be353f1b8de2ae466021e78d78`。
- [x] 安装版 UI 确认“奶昔”/`Taiwan 01` 和新增强制代理控件；代理保持关闭。空闲平均 CPU 0.08%、
  最大 RSS 108,656 KB，无 sing-box/系统代理/recovery，直连 HTTPS 200。
- [ ] 免密码助手因新版 cdhash 显示“需重装”，等待用户确认后手动授权；GitHub 代码、标签和 Release
  尚未推送，线上最新版仍是 v0.1.67。

## 历史版本边界（2026-08-04）

- [x] v0.1.67/build 167 已构建与打包，规则页在当前配置无订阅规则时隐藏无效的“应用订阅规则”开关，
  页头明确说明仍可使用内置分流、广告拦截和分应用代理；保留原有全局偏好，切回带规则配置不会丢选择。
- [x] 全量 `swift test`、无订阅规则离屏快照、deep/strict 签名、DMG 校验、双二进制 arm64 均通过。
- [x] v0.1.67 已安全替换：旧版正常退出、网络恢复检查、完整 M4、可恢复备份、原子安装和重开均通过。
- [x] 最终发布 M4 为 414 通过/2 跳过/0 失败，平均 CPU 0.380%，最大 RSS 116,128 KB。
- [x] DMG SHA-256 为 `0524eeac9e4149fa95ecf734136fd6fbcc18b02ba62bbab8672110201d068854`；
  已安装 App 主程序 SHA-256 为 `e25a6ab7fd234bbd25c12778ac3de938fc730608671980bd8ef566b26a37580b`。
- [x] 配置已备份到 `~/Library/Application Support/kongshan-backups/`，归档校验通过且未进入 Git。
- [x] 构建 App 迁到 `.build`，`dist` 只保留最新 DMG；系统和程序坞仅剩 `/Applications/kongshan.app`。
- [x] 助手真机身份探针 `isTrusted = true`，无需重装，未放宽 cdhash 校验。
- [x] `main` 与 `v0.1.67` 标签已推送；GitHub Release 已发布并上传唯一 DMG，旧 Release/旧版本标签
  已清理。远端只保留 `baseline-20260721` 与 `v0.1.67` 两个标签。

## 2026-08-04 最新运行健康

- [x] 当前 v0.1.67/build 167 以系统代理模式运行，三类系统代理和内核监听均为
  `127.0.0.1:65495`，经代理访问 GitHub 返回 200/约 0.40 秒。
- [x] 10 次两秒采样通过：App 平均/峰值 CPU 0.190%/0.9%，最大 RSS 62,400 KB，FD 85；core FD
  最大 136、ESTABLISHED 最大 102、CLOSE_WAIT 0。
- [x] 24 小时统一日志 0 ERROR/FAULT，近 7 天无崩溃报告；唯一运行事件 error 仍是 8 月 3 日 00:44
  的历史启动失败，之后换网、停止、启动和配置应用均成功。
- [x] 四个订阅的 `autoUpdate` 当前均为 false；TAG 不再自动重试，旧 HTTP 401/TLS 待办关闭。
- [x] Git、DMG 和 GitHub Release 复核通过：分支同步、`dist` 只有最新 DMG，线上唯一资产 digest 与本地一致。
- [ ] 用户手动终验菜单鼠标跟踪，以及真实换网、睡眠唤醒；这些无法由当前只读桌面自动化完整代替。

## 历史版本边界（2026-08-02）

- 已构建、验签、安装并打开 v0.1.66/build 166；替换前确认旧 App/内核退出、系统代理和 DNS 恢复、直连 HTTPS 200，未使用 SIGKILL。
- 成品：`dist/kongshan-0.1.66.dmg`，SHA-256 `67a2e713fee550610e49d8b8634245ef84ac971eae1a66d14c7b6541a649bd1d`。
- 当前测试：414 通过、2 跳过、0 失败；release、M4、arm64、deep/strict 签名、hardened runtime 与 DMG verify 均通过。
- Git 基线为 `97e6153`（v0.1.64）；v0.1.65 状态栏和 v0.1.66 代理页/性能改动均尚未提交。

## v0.1.66 代理页与高频交互优化

- [x] 策略组恢复语义图标；两个测速按钮在 880x560 最小窗口完整显示。
- [x] 代理页复用一次计算的策略、选择、元数据和延迟快照，减少 SwiftUI body 重复工作。
- [x] TCP/URL 测速分别使用 32/16 有界并发；URL 超时 3 秒，结果每 24 条合并发布。
- [x] 141 节点 URL 测速样本约 30 秒降到 17.3 秒，App 峰值 CPU 45.6% 降到 16.8%；
  最终安装版复测因网络响应差异约 26.1 秒完成，界面没有逐节点重排或长时间冻结。
- [x] 当前策略“测速并选最快”不再请求组外节点；配置热重载失败完整回滚 UI 与持久化状态。
- [x] 修复仅内核测速态正常退出后残留 sing-box；最终真机验证 App/core 同时退出、代理/DNS/网络正常。
- [x] 最终 M4 平均 CPU 0.620%，最大 RSS 129,984 KB；安装版重开后保持代理关闭、无内核。

## v0.1.64 菜单稳定性与面板入口

- [x] 状态项不再读取或采样任何周期性网速，只在图标样式或接管状态变化时更新。
- [x] 删除 `statusImage`、菜单栏速率格式化和 AppState 网卡采样链；实时速率保留在仪表盘。
- [x] 面板入口由 App 入口直接注入 `showMainWindow`，不再使用失败时静默无操作的 delegate 强转。
- [x] 2 条定向回归通过；全量 406 通过/1 跳过/0 失败；M4 平均 CPU 0.000%、最大 RSS 126,592 KB。
- [x] 安装版 v0.1.64 PID 92135，系统代理关闭，无 sing-box/恢复文件，直连 HTTPS 200；主窗口正常渲染。
- [ ] 用户手动确认：节点子菜单保持 10 秒不闪烁、可选择节点、关闭主窗口后“打开仪表盘”有效。

## 状态栏实时速度恢复（v0.1.65 已完成）

- [x] 确认周期性替换 `MenuBarExtra` label 会打断原生菜单跟踪，固定宽度或降频不能根治。
- [x] 选定最小结构性方案：仅将托盘迁到 AppKit `NSStatusItem + NSMenu`，其余界面保持 SwiftUI。
- [x] 实现持久化菜单、独立速度按钮刷新和 `NSMenuItem` target/action，并补 5 条原生行为回归测试。
- [x] 完成全量测试、release、M4、DMG 和不强杀旧版的安全替换，状态栏实时速度已恢复。
- [x] M4 平均 CPU 0.200%、最大 RSS 121,184 KB；真实安装版可见窗口平均 CPU 0.630%、最大 RSS 127,712 KB。
- [ ] 用户手动确认：速度持续变化、节点子菜单保持 10 秒、可选择节点、关闭窗口后可从菜单重新打开。

## v0.1.63 菜单节点选择稳定性（历史，未完全解决）

- [x] 下拉菜单正文不再观察每秒变化的代理速率，避免展开期间反复重建 `NSMenu`；状态栏图标实时速率保留。
- [x] 新增 `MenuBarViewStabilityTests` 源码守卫；全量测试 411 通过/1 跳过/0 失败。
- [x] M4 平均 CPU 0.660%、最大 RSS 132,016 KB；helper identifier、路径与 cdhash 探针通过。
- [x] 安装后首次复核系统代理关闭、无 sing-box/恢复文件、直连 HTTPS 200；最终复核时系统代理
  已开启，新版 sing-box PID 74228 监听 `127.0.0.1:65495`，代理 HTTPS 200，接管正常。
- [x] 最终单点 App/core CPU 2.7%/0.9%，RSS 203,952/45,456 KB；本轮未操作代理开关。
- [x] 用户复测确认仍闪烁且“打开仪表盘”无响应；v0.1.64 已继续根治。

## v0.1.62 安装后健康

- [x] `/Applications/kongshan.app` 为 v0.1.62/build 162，PID 68610；系统代理保持关闭，无 sing-box、无恢复文件，直连 HTTPS 200。
- [x] M4 隔离稳态平均 CPU 0.940%、最大 RSS 132016 KB；真实安装版可见仪表盘 10 次平均 CPU 0.180%、最大 RSS 123440 KB。
- [x] 新增长时间只读门禁；短验中 v0.1.61 有真实流量时 App/core FD 111/211、174 ESTABLISHED、0 CLOSE_WAIT。

## v0.1.61 运行健康（2026-08-01 10:35~10:57，历史基线）

- [x] 系统代理接管正常：HTTP/HTTPS/SOCKS 均指向 `127.0.0.1:65495`，sing-box 连续运行，
  helper 连续运行；未发现 App、内核或 helper 崩溃报告。
- [x] 内核资源正常：sing-box 0.4%~0.7% CPU、31 MB；42 条 ESTABLISHED、2 条 LISTEN，
  无 CLOSE_WAIT；core fd 76~80，无持续增长证据。
- [x] 日志未显示持续节点故障：227 条 ERROR 中 150 条为规则主动拒绝；真实故障集中在
  DarkWake 和换网/默认路由消失时，随后自动恢复。
- [x] 旧版正常退出后系统代理、恢复文件和直连网络均验收通过，再安装并打开新版；未强杀旧版。
- [x] 新版代理关闭、窗口可见时平均 CPU 0.100%，最高 0.3%，最大 RSS 150,512 KB。
- [x] 新版有真实流量终验通过：仪表盘平均 2.78%/峰值 4.9%，复测平均 1.45%/峰值 2.5%；
  旧版同场景为 26%~33%。日志突发为最高负载，平均 6.36%/峰值 15.9%，仍在门禁内。
- [x] 全页面采样通过：配置 0.31%、代理 1.15%、规则 0.30%、20 条连接 1.91%、
  136 行日志稳态 0.95%、消息 0.66%、设置 0.18%、菜单栏后台 0.57%。
- [x] 测试后系统内存 61% 可用；App/core FD 98/81，无 CLOSE_WAIT、无新崩溃，
  日志末 2,000 行 0 ERROR/0 WARN，系统代理与 HTTPS 数据面正常。

### 21:44~21:50 长时复查（v0.1.61 历史基线）

- [x] 连接突发期 App 平均 CPU 4.51%、峰值 7.5%、RSS 189~191 MB；安静期平均 3.76%、
  峰值 5.7%、RSS 回落到 184 MB，仍通过 10%/20%/256 MB 门禁。
- [x] sing-box 安静期平均 CPU 0.36%、峰值 0.9%、RSS 30 MB；core FD 73~75、
  39~41 ESTABLISHED、2 LISTEN、0 CLOSE_WAIT。App FD 连续 10 秒固定 108。
- [x] 系统可用内存 58%；无新 App/core 崩溃；17:06 后无真实网络错误，21:23 两条 ERROR
  均为规则主动拒绝。系统代理保持开启，最终代理 HTTPS 返回 200。
- [x] 13:11/13:34 核心启动与订阅、规则、生成配置的落盘时间吻合，现有证据不支持异常崩溃。

## 已实现的能力

**代理与分流**

- [x] Clash YAML 订阅导入、自动更新、用量/到期、配置切换与缓存兜底
- [x] SS（含 SIP003 插件）/ Trojan / VMess / VLESS（Reality·uTLS·Vision）/ Hysteria2 / AnyTLS
- [x] 分享链接粘贴解析（上述六种协议，可批量，坏行跳过）
- [x] 机场策略组、订阅规则、自定义规则、分应用代理、直连/规则/全局
- [x] 系统代理与 TUN 可独立/同时开启；gVisor + Fake-IP 固定；IPv4/IPv6 适配
- [x] 内网 DNS 分流（DHCP 搜索域 → 没有则用内网 DNS 的 PTR 推断 AD 域）

**稳定性**

- [x] 单实例保护（避免两个实例互相写坏系统代理快照）
- [x] 本地 mixed 端口跨启动固定（含被占用时的退避重试）
- [x] 节点域名解析走无连接 UDP（DoH 长连接被 NAT 回收会让整个代理停摆）
- [x] 免密码 TUN helper：签名/cdhash 钉死、配置白名单、孤儿内核认领、单线程无并发
- [x] 系统代理/DNS 快照、失败回滚、启动恢复、换网补挂、睡眠唤醒重建隧道
- [x] 崩溃自愈 + 限流，达到上限时还原系统代理
- [x] 物理网络只有 ULA/链路本地 IPv6 时，TUN 自动移除 IPv6 地址，避免应用新连接走入无默认路由
- [x] 未生效订阅/节点变化不重启当前内核；只有当前运行配置真正变化才走校验、重载与回滚
- [x] 真实换网时用 SystemConfiguration + 物理接口 DHCP 重探 LAN DNS，并重建内核刷新 DoH 传输

**界面**

- [x] 空山图标（App + 菜单栏三种样式可切换）
- [x] 菜单栏原生 `NSStatusItem + NSMenu` 显示整机实时速度；速度刷新与持久化菜单解耦
- [x] 仪表盘本次会话累计流量（跨内核重启连续）
- [x] 规则按目标策略分组折叠
- [x] 内核日志「只看问题」「按连接聚合」，搜索匹配目标主机
- [x] 预期 `block[reject]` 不再算问题；换网根因同一 30 秒桶聚成“网络切换期间”
- [x] 连接页累计合计、链路显示节点名
- [x] 出口 IP / 地区 / 运营商、DNS 泄漏检测、测速并自动选最快
- [x] 消息中心、脱敏诊断导出、备份恢复、缓存管理、开机自启
- [x] 消息页「警告 / 运行事件」时间线；最多 200 条、持久化、定义域脱敏，包含重载前后 PID

**工程**

- [x] arm64 App/DMG 自动构建、版本递增、hardened runtime 与签名验证
- [x] 图标可由脚本重生成（`scripts/make_icons.sh`）
- [x] `verify_m4.sh` 全自动验收
- [x] `verify_dashboard_performance.sh` 对真实安装版做只读可见窗口 CPU/RSS 门禁，失败保存调用栈
- [x] `verify_long_run_health.sh` 只读长时间采样 CPU/RSS/FD/ESTABLISHED/CLOSE_WAIT，超门槛保存诊断
- [x] 构建脚本可选 Developer ID/Keychain/notary profile；无付费账号时保持 ad-hoc + hardened runtime

## 待用户真机验证

- [ ] 睡眠唤醒后隧道自动重建（0.1.40 加入，尚未被真实复现过）
- [ ] TUN 运行中切公司网 / 家庭网 / 手机热点，验证 v0.1.62 的 LAN DNS 重探与 DoH 连接刷新
- [ ] 在**没有透明代理**的网络下验证经代理的出口 IP

## 历史

完整逐阶段记录在 `docs/progress/SESSION_LOG.md`；TUN 真机根因与证据在
`docs/design/tun-real-machine-debug.md`。
